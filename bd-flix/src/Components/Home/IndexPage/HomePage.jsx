import React from 'react';
import Slider from '../IndexSlider/Slider';

const HomePage = () => {
    return (
        <div>
            <Slider></Slider>
        </div>
    );
};

export default HomePage;