import React from 'react';
import Slider from '../IndexSlider/Slider';

const HomePage = () => {
    return (
        <div>
            <p>repo</p>
            <Slider></Slider>
        </div>
    );
};

export default HomePage;